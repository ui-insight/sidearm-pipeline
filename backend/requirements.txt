# Authoritative install list for local setup, CI, containers, and security scans.
# Keep package names aligned with backend/pyproject.toml, which records the
# minimum supported versions for backend packaging metadata.
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy[asyncio]==2.0.52
pydantic==2.13.5
pydantic-settings==2.15.0
pyjwt[crypto]==2.13.0
bcrypt==5.0.0
aiosqlite==0.22.1
asyncpg==0.31.0
python-multipart==0.0.32
alembic==1.19.1
httpx==0.28.1
beautifulsoup4==4.15.0
lxml==6.1.2
anthropic==0.119.0

# Development / Testing pins used by contributors and CI.
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
ruff==0.16.5
